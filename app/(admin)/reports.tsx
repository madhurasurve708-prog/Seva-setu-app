import { Alert, Pressable, ScrollView, Text, View } from 'react-native';
import { AdminShell } from '@/components/admin/admin-shell';
import { useOfficial } from '@/providers/official-provider';
export default function ReportsScreen() { const { complaints } = useOfficial(); const generate = (type: string) => Alert.alert('Report queued', `${type} will download once the municipal reporting endpoint is available.`); return <AdminShell title="Reports"><ScrollView contentContainerStyle={{ padding: 16 }}><Text className="text-sm text-slate-500 mb-4">Generate an export using the active complaint filters and administrator permissions.</Text>{[['Ward performance', 'Ward-wise resolution and pending work'], ['Department performance', 'Assignment and resolution trends'], ['Escalation register', 'Escalation history and action owners'], ['Monthly complaint register', `${complaints.length} complaint records in current data`]].map(([title, detail]) => <View key={title} className="bg-white border border-slate-100 rounded-2xl p-4 mb-3"><Text className="font-bold text-slate-800">{title}</Text><Text className="text-xs text-slate-500 mt-1">{detail}</Text><Pressable onPress={() => generate(title)} className="mt-3 self-start bg-blue-50 px-3 py-2 rounded-xl"><Text className="text-blue-600 text-xs font-bold">Generate report</Text></Pressable></View>)}</ScrollView></AdminShell>; }
