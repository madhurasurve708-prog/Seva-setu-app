import { useRouter } from 'expo-router';
import { Pressable, ScrollView, Text, View } from 'react-native';
import { AdminShell } from '@/components/admin/admin-shell';
import { useOfficial } from '@/providers/official-provider';
export default function AdminProfile() { const { profile } = useOfficial(); const router = useRouter(); const rows = [['Mobile Number', profile.phone], ['Username', profile.username], ['Employee ID', profile.employeeId], ['Department', profile.department], ['Designation', profile.designation], ['Ward Access', profile.ward]]; return <AdminShell title="Profile"><ScrollView contentContainerStyle={{ padding: 16 }}><View className="bg-white rounded-3xl p-6 border border-slate-100 items-center"><View className="w-20 h-20 bg-blue-600 rounded-full items-center justify-center"><Text className="text-white text-3xl font-extrabold">{profile.avatarInitial}</Text></View><Text className="text-xl font-extrabold text-slate-800 mt-3">{profile.name}</Text><Text className="text-slate-500 text-sm">{profile.roleLabel}</Text></View><View className="bg-white rounded-3xl p-4 border border-slate-100 mt-4">{rows.map(([label, value]) => <View key={label} className="py-3 border-b border-slate-100"><Text className="text-xs font-bold text-slate-400">{label}</Text><Text selectable className="font-semibold text-slate-800 mt-1">{value || 'Not provided'}</Text></View>)}</View><Pressable onPress={() => router.push('/(official)/profile' as any)} className="bg-blue-600 rounded-xl py-3 mt-4 items-center"><Text className="text-white font-bold">Edit profile & change password</Text></Pressable></ScrollView></AdminShell>; }
