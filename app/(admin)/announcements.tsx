import { Ionicons } from '@expo/vector-icons';
import { useState } from 'react';
import { Alert, Pressable, ScrollView, Text, TextInput, View } from 'react-native';
import { AdminShell } from '@/components/admin/admin-shell';
import { useOfficial } from '@/providers/official-provider';
export default function AdminAnnouncements() { const { announcements } = useOfficial(); const [title, setTitle] = useState(''); const [message, setMessage] = useState(''); const [audience, setAudience] = useState('Everyone'); const [priority, setPriority] = useState('Normal'); const send = () => { if (!title.trim() || !message.trim()) return Alert.alert('Missing information', 'Enter both a title and announcement message.'); Alert.alert('Ready to send', `${priority} announcement will be sent to ${audience} when the backend endpoint is connected.`); setTitle(''); setMessage(''); }; return <AdminShell title="Announcements"><ScrollView contentContainerStyle={{ padding: 16, paddingBottom: 40 }}><View className="bg-white rounded-3xl border border-slate-100 p-4 mb-5"><Text className="font-extrabold text-slate-800 mb-3">Create announcement</Text><TextInput value={title} onChangeText={setTitle} placeholder="Announcement title" className="border border-slate-200 rounded-xl px-3 py-3 text-slate-800 mb-3"/><TextInput value={message} onChangeText={setMessage} placeholder="Message" multiline className="border border-slate-200 rounded-xl px-3 py-3 text-slate-800 h-24 mb-3"/><ScrollView horizontal showsHorizontalScrollIndicator={false} className="mb-3">{['Everyone', 'All Citizens', 'Specific Ward Citizens', 'All Nagarsevaks', 'Department Officers'].map(value => <Pressable key={value} onPress={() => setAudience(value)} className={`mr-2 px-3 py-2 rounded-full ${audience === value ? 'bg-blue-600' : 'bg-slate-100'}`}><Text className={audience === value ? 'text-white text-xs font-bold' : 'text-slate-600 text-xs font-bold'}>{value}</Text></Pressable>)}</ScrollView><View className="flex-row mb-4">{['Normal', 'Important', 'Emergency'].map(value => <Pressable key={value} onPress={() => setPriority(value)} className={`mr-2 px-3 py-2 rounded-full ${priority === value ? 'bg-orange-500' : 'bg-slate-100'}`}><Text className={priority === value ? 'text-white text-xs font-bold' : 'text-slate-600 text-xs font-bold'}>{value}</Text></Pressable>)}</View><Pressable onPress={send} className="bg-[#0A2A43] rounded-xl py-3 items-center"><Text className="text-white font-bold">Send announcement</Text></Pressable></View><Text className="font-extrabold text-slate-800 mb-3">Published announcements</Text>{announcements.map(a => <View key={a.id} className="bg-white rounded-2xl border border-slate-100 p-4 mb-3"><View className="flex-row justify-between"><Text className="font-bold text-slate-800 flex-1">{a.title}</Text><Ionicons name="megaphone-outline" size={18} color="#1E6FD9" /></View><Text className="text-xs text-slate-500 mt-2">{a.body}</Text><Text className="text-[11px] text-slate-400 mt-3">{a.priority} • {a.date}</Text></View>)}</ScrollView></AdminShell>; }
