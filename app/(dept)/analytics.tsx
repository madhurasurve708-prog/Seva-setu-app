import { ScrollView, StyleSheet, Text, View } from 'react-native';
import { DepartmentScreen } from '@/components/dept/department-screen'; import GlassCard from '@/components/common/GlassCard'; import { COLORS } from '@/constants/theme'; import { useDepartment } from '@/providers/department-provider';
export default function DepartmentAnalytics(){const {profile,complaints}=useDepartment();const mine=complaints.filter(c=>c.assignedDepartment===profile?.department&&!c.is_deleted);const resolved=mine.filter(c=>c.status==='Resolved').length;const wards=Array.from(new Set(mine.map(c=>c.ward)));const categories=Array.from(new Set(mine.map(c=>c.category)));const cards=[['Pending',mine.filter(c=>c.status==='Pending').length],['Resolved',resolved],['Avg. resolution','2.4 days'],['Performance',mine.length?`${Math.round(resolved/mine.length*100)}%`:'0%']];return <DepartmentScreen title="Department Analytics" tab="analytics"><ScrollView contentContainerStyle={styles.content}><Text style={styles.scope}>Department-only reporting · all wards</Text><View style={styles.grid}>{cards.map(([label,value])=><GlassCard key={label} style={styles.stat}><Text style={styles.value}>{value}</Text><Text style={styles.label}>{label}</Text></GlassCard>)}</View><Text style={styles.title}>Monthly performance</Text><GlassCard style={styles.card}><Text style={styles.row}>July 2026</Text><View style={styles.bar}><View style={[styles.fill,{width:`${mine.length?Math.round(resolved/mine.length*100):0}%`} ]}/></View><Text style={styles.sub}>{resolved} resolved from {mine.length} total complaints</Text></GlassCard><Text style={styles.title}>Ward-wise</Text><GlassCard style={styles.card}>{wards.map(ward=><View key={ward} style={styles.line}><Text style={styles.row}>{ward}</Text><Text style={styles.count}>{mine.filter(c=>c.ward===ward).length}</Text></View>)}</GlassCard><Text style={styles.title}>Category-wise</Text><GlassCard style={styles.card}>{categories.map(category=><View key={category} style={styles.line}><Text style={styles.row}>{category}</Text><Text style={styles.count}>{mine.filter(c=>c.category===category).length}</Text></View>)}</GlassCard></ScrollView></DepartmentScreen>};const styles=StyleSheet.create({content:{padding:16,paddingBottom:24,gap:12},scope:{fontSize:12,fontWeight:'700',color:COLORS.primary},grid:{flexDirection:'row',flexWrap:'wrap',gap:10},stat:{width:'47%',flexGrow:1,padding:15},value:{fontSize:22,fontWeight:'900',color:COLORS.primary},label:{fontSize:11,color:COLORS.textMuted,fontWeight:'700',marginTop:3},title:{fontSize:15,fontWeight:'800',color:COLORS.text,marginTop:6},card:{padding:15,gap:11},row:{fontSize:13,fontWeight:'700',color:COLORS.text},sub:{fontSize:12,color:COLORS.textMuted},bar:{height:10,backgroundColor:'#E5EAF0',borderRadius:5,overflow:'hidden'},fill:{height:'100%',backgroundColor:COLORS.success,borderRadius:5},line:{flexDirection:'row',justifyContent:'space-between'},count:{fontWeight:'900',color:COLORS.primary}});
